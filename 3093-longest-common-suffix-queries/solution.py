# Longest Common Suffix Queries
# Difficulty: Hard
# Runtime: 4586 ms
# Memory: 340.9 MB
# https://leetcode.com/problems/longest-common-suffix-queries/

               len(word) < len(wordsContainer[root.best_idx]) or \
            if root.best_idx == -1 or \
            # Update root with best candidate (shortest len, then earliest idx)
            node = root
        for i, word in enumerate(wordsContainer):
        
        root = TrieNode()
        # Build the trie on reversed container words
    def stringIndices(self, wordsContainer, wordsQuery):
class Solution:

        self.best_idx = -1  # stores best candidate at this node
        self.children = {}
    def __init__(self):
class TrieNode:
