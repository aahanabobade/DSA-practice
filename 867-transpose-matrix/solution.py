# Transpose Matrix
# Difficulty: Easy
# Runtime: 0 ms
# Memory: 12.9 MB
# https://leetcode.com/problems/transpose-matrix/

            row = []
            for i in range(m):
                row.append(matrix[i][j])
            result.append(row)
            
        for j in range(n):
        
        
        result = []
        m = len(matrix)
        n = len(matrix[0])
        """
