# Count the Number of Special Characters II
# Difficulty: Medium
# Runtime: 312 ms
# Memory: 14.9 MB
# https://leetcode.com/problems/count-the-number-of-special-characters-ii/

                if lower not in firstUpper:


            else:
                lower = ch.lower()
            if ch.islower():
                lastLower[ch] = i

        for i, ch in enumerate(word):

        firstUpper = {}
        lastLower = {}

        """
        :rtype: int
        :type word: str
