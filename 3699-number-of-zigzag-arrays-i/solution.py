# Number of ZigZag Arrays I
# Difficulty: Hard
# Runtime: 11228 ms
# Memory: 12.9 MB
# https://leetcode.com/problems/number-of-zigzag-arrays-i/


        dp0 = [1] * m
        dp1 = [1] * m
        MOD = 1000000007
        m = r - l + 1
    def zigZagArrays(self, n, l, r):
            res.append(s)
        return res

        for x in arr:
            s += x
        s = initial
        res = [initial]
    def accumulate_with_initial(self, arr, initial=0):
class Solution(object):
